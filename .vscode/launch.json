{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package (WSL)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/main.go",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "env": {},
      "args": ["hugo", "markdown-link-checker"]
    }
  ]
}